# Calculator API                   # Project title so GitHub renders a nice header.
Simple Flask API with add/subtract # One-line purpose/summary a maintainer or reviewer sees first.
endpoints, containerized with      # Clarifies it's Dockerized for predictable runs.
Docker and deployed to Azure.      # Notes intended deployment target (Azure App Service with ACR).
